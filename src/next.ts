/**
 * Punto de entrada específico de Next.js App Router (`next/navigation`).
 * Aparte de `.` a propósito: ese entry es para cualquier framework que
 * compile JSX, y no debe arrastrar una dependencia de Next.js para quien
 * no la necesita.
 */
export { MultiAgentChat } from "./MultiAgentChat";
